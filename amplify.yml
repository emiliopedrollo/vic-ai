version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 'yarn set version 4.1.1'
        - 'yarn install'
    build:
      commands:
        - 'yarn run build'
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*'
